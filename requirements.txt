# Core Django
Django==6.0
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==24.3
django-storages==1.14.6
drf-nested-routers==0.93.5

# Deployment (daphne, in "Async & WebSocket" below, is the production ASGI server)
whitenoise==6.8.2
dj-database-url==2.3.0

# Database
psycopg2-binary==2.9.11

# Async & WebSocket
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0
uvicorn==0.40.0

# Redis & Cache (versiones compatibles probadas)
redis==4.6.0
django-redis==5.2.0

# AWS
boto3==1.42.2
botocore==1.42.2
s3transfer==0.16.0

# Auth & Security
PyJWT==2.10.1

# Utilities
python-decouple==3.8
python-dateutil==2.9.0.post0
pillow==12.0.0
jmespath==1.0.1
sqlparse==0.5.4

# Machine Learning & NLP (HU2: Sistema de Recomendación)
# Floors chosen so prebuilt wheels exist on Python 3.12 (Docker) AND 3.13 (local).
# numpy<2 has no cp313 wheels and would force a source build (needs a C++ compiler).
numpy>=2.1.0
scikit-learn>=1.6.0
nltk>=3.8.1
joblib>=1.3.2
scipy>=1.14.1